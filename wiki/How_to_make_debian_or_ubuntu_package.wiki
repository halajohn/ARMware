=============================================================================

  # Put ARMware source code into an empty directory.
  # Enter into this directory.
  # Execute "dpkg-buildpackage -rfakeroot"
  # You can use "lintian -i armware-1_i386.changes" to check correctness.

=============================================================================

  # 將 ARMware 的 source codes 放在一個空的目錄下
  # 進入 ARMware 目錄
  # 執行 dpkg-buildpackage -rfakeroot
  # 可用 lintian -i armware-1_i386.changes 來檢察錯誤.

=============================================================================